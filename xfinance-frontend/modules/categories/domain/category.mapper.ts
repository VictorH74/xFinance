// import { Category } from "./category.types";

// export function mapCategoryToCard(transaction: Category) {
//   return {
//     id: transaction.id,
//     title: transaction.description,
//     amountLabel:
//       transaction.type === "income"
//         ? `+$${transaction.amount}`
//         : `-$${transaction.amount}`,
//     date: transaction.date,
//   };
// }
