// import { Transaction } from "./transaction.types";

// export function mapTransactionToCard(transaction: Transaction) {
//   return {
//     id: transaction.id,
//     title: transaction.description,
//     amountLabel:
//       transaction.type === "income"
//         ? `+$${transaction.amount}`
//         : `-$${transaction.amount}`,
//     date: transaction.date,
//   };
// }
